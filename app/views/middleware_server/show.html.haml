- if %w(middleware_datasources middleware_deployments).include?(@display)
  = render :partial => "layouts/gtl", :locals => {:action_url => "show/#{@record.id}"}
- elsif @showtype == "performance"
  = render(:partial => "layouts/performance")
  :javascript
    var miq_after_onload = "miqAsyncAjax('#{url_for(:action => @ajax_action, :id => @record)}');"
- else
  = render :partial => @showtype
= render :partial => 'deploy'
= button_tag(t = _('Submit'),
      :class   => "btn btn-primary",
      :alt     => t,
      :title   => t,
      :onclick => "miqAjaxButton('#{url_for(:action => "add_deployment")}');")
%div{"ng-show" => "blabla"} hello g
